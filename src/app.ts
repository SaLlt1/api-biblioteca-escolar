// Configuracao principal do Express (app)
// - view engine EJS
// - middlewares globais (express.json, express.urlencoded, session, static)
// - registro das rotas (auth, livros, alunos, emprestimos)
// - export default app (sem dar listen aqui, para facilitar testes)
