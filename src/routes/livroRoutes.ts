// Rotas de Livro (CRUD)
// GET    /livros           -> listar (view + api)
// GET    /livros/novo      -> form de cadastro
// POST   /livros           -> criar (com upload de imagem via Multer)
// GET    /livros/:id       -> detalhes
// GET    /livros/:id/editar-> form de edicao
// PUT    /livros/:id       -> atualizar
// DELETE /livros/:id       -> remover
// Rotas protegidas por middleware de autenticacao
