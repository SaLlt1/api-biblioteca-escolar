// Entidade Livro (classe OOP)
// Deve conter: atributos privados, getters/setters, metodo validar(), fromJSON() e toJSON()
// Atributos sugeridos: id, titulo, autor, isbn, categoria, quantidade, imagem
